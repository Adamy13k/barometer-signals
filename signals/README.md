# Daily signal archive

One Markdown + JSON pair per day, generated from the live paper book and
committed daily at ~04:30 UTC. The git commit timestamps are the verification:
signals for date N are committed before many of date N's markets resolve, and
every historical file is immutable in the repo history.
